import React from "react";
import ReactDOM from "react-dom/client";
import App from "./App";

const juuri = ReactDOM.createRoot(document.getElementById("root"));

juuri.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
